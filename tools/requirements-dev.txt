# Development tools only. Nothing here ships - the app is a static web page
# that runs entirely in the browser.
#
# What these are for:
#   pillow, numpy  - writing the test fixtures and the validation vectors
#   fonttools[woff]- subsets the shipped faces and reads their x-height;
#                    tools/gen_fonts.py --check needs it, so the suite does.
#                    The [woff] extra is not optional: it brings brotli, and
#                    without it opening a .woff2 raises ImportError
#   ssimulacra2    - the REFERENCE implementation of the quality metric.
#                    tests/web/ss2_validate.mjs holds web/ss2.js to it, which
#                    is what keeps the browser's score honest.
#   scipy          - the reference metric's Gaussian blur
#   ruff           - lint
#
# Install:  python -m pip install -r tools/requirements-dev.txt
pillow>=10.0
fonttools[woff]>=4.40
numpy>=1.24
ssimulacra2>=0.3
scipy>=1.10
ruff>=0.5
