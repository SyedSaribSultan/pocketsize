__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.ruff_cache/
.pytest_cache/
.DS_Store
Thumbs.db

# working folders - keep the folders, ignore what lands in them
input/*
output/*
!input/.gitkeep
!output/.gitkeep

# generated benchmark corpus
tests/fixtures/
tests/video_fixtures/
tests/real_world_fixtures/

# vercel
.vercel/

# scraped research scratch. What was concluded from it belongs in docs/ -
# THIRD_PARTY_NOTICES.md carries the licensing findings - so the
# raw pages are working notes, not repo content.
.firecrawl/

tests/*.log

# Downloaded font sources, re-fetched on demand by tools/gen_fonts.py.
# The subset woff2 files it PRODUCES are committed; these originals are not,
# because they are several megabytes and reproducible from the family name.
.fontcache/
